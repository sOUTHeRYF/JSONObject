using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace JSONObject
{
    class SeriaObject
    {
        private static readonly char[] stringPairChar = { '\"', '\"' };
        private static readonly char[] listPairChar = { '[', ']' };
        private static readonly char[] dicPairChar = { '{', '}' };
    }
}
